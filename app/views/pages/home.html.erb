<% if current_user.present? %>

  <div class="container" id="tweetsContainer">
    <div class="col-md-10">

      <h1>You've currently made <%= current_user.posts.count %> posts:</h1>

      <% current_user.posts.each_with_index do |post, index| %>
        <div class="container">
          <p>
          <%= index+1 %>)
          <%= post.content %>
          <%= link_to "Delete", post_path(post), method: :delete %>
          </p>
        </div>
      <% end %>

      <!-- Modal button -->
      <button type="button" id="modalbutton" onclick="launchModal()" class="modalbutton btn btn-primary" data-bs-toggle="modal" data-bs-target="#tweetModal" >
        New Tweet
      </button>

    </div>


  </div>

<% end %>

<!-- Covid -->
<div class="container my-3">

  <div class="col-md-6">

    <h1>Covid Data</h1>
    <p >Date: <span id="covid-date">Fetching data..</span></p>
    <p >New Confirmed: <span id="covid-new-confirmed">Fetching data..</span></p>
    <p >TotalConfirmed: <span id="covid-total-confirmed">Fetching data..</span></p>
    <p >NewDeaths: <span id="covid-new-deaths">Fetching data..</span></p>
    <p >TotalDeaths: <span id="covid-total-deaths">Fetching data..</span></p>
    <p >NewRecovered: <span id="covid-new-recovered">Fetching data..</span></p>
    <p >TotalRecovered: <span id="covid-total-recovered">Fetching data..</span></p>

  </div>

</div>

<%= render "posts/modal" %>
